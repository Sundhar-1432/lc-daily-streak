class Solution {
public:
    string clearDigits(string s) {
        std::string res;
        for(auto c:s){
            if(c>='0'&&c<='9')res.pop_back();
            else res.push_back(c);
        }
        return res;
    }
};
